const templateLibrary=document.querySelector('.template-library');
if(templateLibrary){
  const source='/images/hero/cheeto-desert-sunset-1440.jpg';
  const controls=document.createElement('div');controls.className='template-controls';controls.innerHTML='<div class="format-line"><span><b>Photo format</b><small>Detected automatically from the selected photo</small></span><button id="formatToggle">Auto · Landscape</button></div><div class="finished-image landscape"><img class="finished-photo" src="'+source+'" alt="Finished Trail Journal image preview"><img class="finished-logo" alt="Selected Trail Journal treatment"><em>FINISHED ARTICLE IMAGE</em></div><div class="photo-set"><b>Story image 1 of 4</b><span><button class="active">1</button><button>2</button><button>3</button><button>4</button></span></div><label class="apply-all"><input type="checkbox" checked> Apply this treatment to all four story images</label><div class="adjustments" hidden><label class="crop-control">Focal point<input id="cropPosition" type="range" min="0" max="100" value="50"></label><div class="mini-controls"><label>Logo size<select id="logoSize"><option>Small</option><option selected>Medium</option></select></label><label>Logo side<select id="logoSide"><option selected>Left</option><option>Right</option></select></label></div><label>Editable text<input id="templateText" placeholder="Optional"></label><small>Every change is stored as a working version. The original photo remains untouched.</small></div>';
  templateLibrary.prepend(controls);
  controls.querySelector('.mini-controls').insertAdjacentHTML('afterbegin','<label>Logo color<select id="logoColor"><option>Bark</option><option>Sand</option><option>Sage</option><option>Terracotta</option></select></label>');
  const groups=[...templateLibrary.querySelectorAll('.template-groups section')],toggle=controls.querySelector('#formatToggle'),result=controls.querySelector('.finished-image'),photo=controls.querySelector('.finished-photo'),logo=controls.querySelector('.finished-logo'),range=controls.querySelector('#cropPosition'),adjustments=controls.querySelector('.adjustments'),size=controls.querySelector('#logoSize'),side=controls.querySelector('#logoSide');let orientation='landscape',color=null;
  const applyAll=controls.querySelector('.apply-all input'),textField=controls.querySelector('#templateText'),versions=Array.from({length:4},()=>({color:null,size:'Medium',side:'Left',focal:'50',text:''}));let currentImage=0;
  function prepareTray(group){const grid=group.querySelector('.template-grid'),original=document.createElement('button');original.className='template-card original selected';original.innerHTML='<span><img class="treatment-photo" src="'+source+'" alt="Original photo"></span><b>Original</b><i>✓</i>';grid.prepend(original);grid.querySelectorAll('.template-card:not(.original)').forEach(button=>{const overlay=button.querySelector('img'),frame=button.querySelector('span');frame.innerHTML='<img class="treatment-photo" src="'+source+'" alt=""><img class="treatment-overlay" src="'+overlay.src+'" alt="">';button.insertAdjacentHTML('beforeend','<i>✓</i>')})}
  groups.forEach(prepareTray);
  const colorChoice=controls.querySelector('#logoColor');function persist(){const version={color,size:size.value,side:side.value,focal:range.value,text:textField.value};if(applyAll.checked)versions.forEach((_,index)=>versions[index]={...version});else versions[currentImage]=version}function loadVersion(){const version=versions[currentImage];color=version.color;size.value=version.size;side.value=version.side;range.value=version.focal;textField.value=version.text;photo.style.objectPosition=`${version.focal}% 50%`;apply();templateLibrary.querySelectorAll('.template-card').forEach(item=>{const itemColor=[...item.classList].find(name=>['bark','sand','sage','terracotta'].includes(name));item.classList.toggle('selected',!item.closest('section').hidden&&(color?itemColor===color:item.classList.contains('original')))})}colorChoice.onchange=()=>{color=colorChoice.value.toLowerCase();persist();apply()};
  function apply(){groups.forEach(group=>group.hidden=!group.querySelector(`[data-orientation="${orientation}"]`));result.classList.toggle('landscape',orientation==='landscape');result.classList.toggle('portrait',orientation==='portrait');logo.hidden=!color;logo.src=color?`/images/templates/trail-journal/${orientation}-${color}.png`:'';logo.className=`finished-logo ${size.value.toLowerCase()} ${side.value.toLowerCase()}`;adjustments.hidden=!color;toggle.textContent=`Auto · ${orientation[0].toUpperCase()+orientation.slice(1)}`}
  templateLibrary.querySelectorAll('.template-card').forEach(button=>button.onclick=()=>{templateLibrary.querySelectorAll('.template-card').forEach(item=>item.classList.remove('selected'));button.classList.add('selected');color=button.classList.contains('original')?null:[...button.classList].find(item=>['bark','sand','sage','terracotta'].includes(item));if(color)colorChoice.value=color[0].toUpperCase()+color.slice(1);persist();apply()});
  toggle.onclick=()=>{orientation=orientation==='landscape'?'portrait':'landscape';color=null;persist();apply();templateLibrary.querySelectorAll('.template-card').forEach(item=>item.classList.toggle('selected',item.classList.contains('original')&&!item.closest('section').hidden))};range.oninput=()=>{photo.style.objectPosition=`${range.value}% 50%`;persist()};size.onchange=()=>{persist();apply()};side.onchange=()=>{persist();apply()};textField.oninput=persist;controls.querySelectorAll('.photo-set button').forEach((button,index)=>button.onclick=()=>{currentImage=index;controls.querySelectorAll('.photo-set button').forEach(item=>item.classList.toggle('active',item===button));controls.querySelector('.photo-set>b').textContent=`Story image ${index+1} of 4`;loadVersion()});apply();
}
