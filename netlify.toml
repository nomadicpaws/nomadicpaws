[build]
  publish = "."

[functions]
  directory = "netlify/functions"
  # Bundle the CMS-authored Markdown posts so the posts API can read them.
  included_files = ["_posts/**"]
[[redirects]]
  from = "/trail-journal/:slug"
  to = "/post.html?slug=:slug"
  status = 200

[[redirects]]
  from = "/trail-journal"
  to = "/blog.html"
  status = 200
